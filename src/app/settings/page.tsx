
export default function Settings(){
  return (
    <div>
      <h1>Configurações</h1>
      <p>MVP usando JSONs locais e Postgres no Railway (Nixpacks). Ajuste variáveis para produção.</p>
    </div>
  );
}
